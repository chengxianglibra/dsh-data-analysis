/** Real Agent + Harness Python execution admission, with isolated fixture credentials. */
import { runCredentialValidation } from './credential-validation-fixture.ts'

await runCredentialValidation(true)
