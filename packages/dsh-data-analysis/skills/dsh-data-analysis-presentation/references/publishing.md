# 发布报告链接

当用户明确要求发布到对象存储且实时工具中存在 `marivo_publish_report` 时，用已保存 receipt 的 `report_id` 和 `build_id` 调用。成功后返回工具确认的 URL；凭据缺失时引导用户到“数据源与凭证 → 报告发布凭证”配置，不索取或传递密钥。上传结果未确认不宣称成功；可重试同一保存版本。开启发布时，在线报告菜单将完整报告和当前视图的 HTML 下载替换为上传；当前筛选视图由用户在阅读器发布，不通过 Tool 猜测。
